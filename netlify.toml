
# Build settings
[build]
  base = "/opt/build/repo"  # Specify the correct base directory path
  command = "npm run build"
  commandOrigin = "config"
  publish = "build"
  publishOrigin = "config"

# Redirects
[[redirects]]
  from = "/*"
  status = 200
  to = "/index.html"





# # Build settings
# [build]
#   base = "/"
#   command = "npm run build"
#   publish = "/build"

# # Redirects
# [[redirects]]
#   from = "/*"
#   status = 200
#   to = "/index.html"
